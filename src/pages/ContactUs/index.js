import {Page} from '../../components';
import {PAGES} from '../../utils/constants';

const ContactUs = props => {
  return (
    <Page pageName={PAGES.CONTACT_US}>

    </Page>
  )
}

export default ContactUs;
